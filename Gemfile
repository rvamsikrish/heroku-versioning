source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in pwa_app_version.gemspec.
gemspec

group :development do
  gem 'puma'
  gem 'sqlite3'
end

gem 'pry'
# To use a debugger
# gem 'byebug', group: [:development, :test]
